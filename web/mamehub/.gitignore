# Emscripten build outputs (copy from repo root after emmake / rebuild_fast.sh)
dist/*
!dist/.gitkeep

# Candy proxy ROM downloads (serve.py cache)
.candy-cache/

# Windows package output
release/

# Local Go launcher binary
launcher/mamehub-launcher
launcher/MAMEHubOnline.exe

# npm
node_modules/
package-lock.json
